#include "main.h"
/**
 * _islower - program checks for lowercase character
 * @c: character to check
 * Return: 1 if c is lowecase, and 0 if its not
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
