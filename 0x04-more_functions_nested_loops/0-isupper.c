#include "main.h"

/**
 * _isupper - Check if a letter is upper
 * @x: Variable text
 * Return: Always 0.
 */
int _isupper(int x)
{
	if (x >= 'A' && x <= 'Z')
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
